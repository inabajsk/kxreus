# PlatformIO's build tree and downloaded libraries. Hundreds of megabytes,
# and `pio run` recreates all of it.
.pio/

# Wi-Fi credentials for tools/wifi_setup.py. Never committed.
tools/.env

# Python bytecode cache, regenerated on every run of any tools/*.py.
tools/__pycache__/
